import React from "react";
// import ProposalForm, TransactionTable, etc. as implemented

const SenderDashboard: React.FC = () => {
  // TODO: Fetch proposals, transactions, and sender views from store
  // TODO: Implement create proposal, settle, cancel actions
  return (
    <div className="container mt-4">
      <h2>Sender Dashboard</h2>
      {/* <ProposalForm /> */}
      {/* <TransactionTable role="sender" /> */}
      {/* Add buttons/actions for settle, cancel */}
      <p>Sender actions: create proposals, settle, cancel.</p>
    </div>
  );
};

export default SenderDashboard;
